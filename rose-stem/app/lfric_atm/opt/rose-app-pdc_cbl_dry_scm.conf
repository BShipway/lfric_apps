# PDC Paper Test 7.5b: SCM Dry Convective Boundary Layer
# =======================================================
# Complementary SCM test with a dry convective boundary layer case.
# Validates that face-normal wind tendencies produce the same vertical
# diffusion profiles as the equivalent FD column model.
#
# Uses the existing lfric_atm cbl_dry configuration.
#
# Mechanisms exercised: A (DOF injection), B (column segmentation),
#                       D (H-div velocity), E (SI coupling)

# This file is a marker — the actual configuration uses:
#   lfric_atm with opt_confs: BiP2x2-50000x50000, l110_4p4km, cbl_dry, ral3_scm
# These all exist in lfric_apps/rose-stem/app/lfric_atm/opt/

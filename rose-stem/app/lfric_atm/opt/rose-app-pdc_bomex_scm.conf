# PDC Paper Test 7.5: SCM Boundary-Layer Test (BOMEX)
# ====================================================
# Uses the lfric_atm mini-app with SCM (single-column model) configuration.
#
# The BOMEX trade-wind cumulus case exercises:
# - Direct face-normal wind injection (Pathway A, no J transform)
# - BL tendency injection into the SI RHS
# - Column segmentation (DOMAIN mode physics)
# - Inter-space conversion for physics scalars
#
# This references the existing lfric_atm bomex configuration.
# We set lfric_atm opt configs: bomex + BiP2x2-50000x50000 + ral3_scm
#
# Mechanisms exercised: A (DOF injection), B (column segmentation),
#                       C (inter-space conversion), D (H-div velocity),
#                       E (SI coupling)
# Reference: Siebesma et al. (2003) BOMEX

# This file is a marker — the actual configuration uses:
#   lfric_atm with opt_confs: BiP2x2-50000x50000, l110_4p4km, bomex, ral3_scm
# These all exist in lfric_apps/rose-stem/app/lfric_atm/opt/
